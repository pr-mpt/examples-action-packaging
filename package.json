{
  "main": "lib/main.js",
  "scripts": {
    "compile": "tsc",
    "package": "npm run compile && ncc build --source-map --license licenses.txt",
    "test": "jest",
    "all": "npm run package && npm run test"
  },
  "devDependencies": {
    "@actions/core": "^1.3.0",
    "@types/jest": "^26.0.15",
    "@types/node": "^14.14.9",
    "@vercel/ncc": "^0.25.1",
    "jest": "^26.6.3",
    "jest-circus": "^26.6.3",
    "js-yaml": "^3.14.0",
    "ts-jest": "^26.4.4",
    "typescript": "^4.1.3"
  }
}
